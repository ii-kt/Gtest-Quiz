{
  "name": "classroom",
  "mode": "trusted-lan",
  "host": "0.0.0.0",
  "port": 8000,
  "session_ttl_hours": 12,
  "sqlite_path": ".runtime/classroom_quiz.db",
  "cors": ["http://localhost", "http://127.0.0.1"],
  "backup": {
    "export_before_shutdown": true,
    "retain_audit_days": 30
  },
  "notes": "Use for workshops where learners connect from the same trusted network."
}
