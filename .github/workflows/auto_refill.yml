name: Legacy Auto Refill Question Bank (Disabled)

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement workflow
        run: |
          echo "Legacy auto_refill is disabled."
          echo "Use 'Auto Refill Question Bank (Quality Pipeline)' for scheduled or manual generation."
